HTTP:
HTTP (HyperText Transfer Protocol) é um protocolo de comunicação entre cliente e servidor. O HTTP funciona seguindo um modelo de solicitação-resposta, o que significa que as requisições são solicitadas pelo destinatário.
O HTTP funciona seguindo um modelo de solicitação-resposta. Um cliente envia uma solicitação HTTP para um servidor, especificando o método de solicitação (como GET, POST, PUT, DELETE) e o recurso desejado (como uma página da web ou um serviço). O servidor recebe a solicitação e envia uma resposta HTTP contendo um código de status (indicando se a solicitação foi bem-sucedida ou não) e os dados solicitados, se aplicável.
